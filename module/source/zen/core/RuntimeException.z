/**
 * @author Samuel Rowe
 * @since  1.0
 */
class RuntimeException

    function new(cause) : Throwable(cause)
        ;

    function new(cause, message) : Throwable(cause, message)
        ;